const calculateArea = function(length, width) {
    return length * width;
};

let length = 10;
let width = 5;
const area = calculateArea(length, width);
console.log(`The area of the rectangle is ${area} square units`); // Outputs: The area of the rectangle is 50 square units
